trigger:
- master
- develop

steps:
- task: VSBuild@1
  inputs:
    solution: '**\*.sln'
    msbuildArgs: '/m'
    platform: 'x64'
    configuration: 'Release'
    restoreNugetPackages: true
    msbuildArchitecture: 'x64'
- task: DotNetCoreCLI@2
  inputs:
    command: 'test'
